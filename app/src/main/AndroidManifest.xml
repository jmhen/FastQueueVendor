<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jem.fq">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginPage"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QueueMain" />
        <activity android:name=".preorder.PreorderMain" />
        <activity android:name=".setting.SettingsMain"
            android:label="@string/app_name"></activity>
        <activity android:name=".setting.SettingsMenu" />
        <activity android:name=".SettingsProfile" />
        <activity android:name=".SettingsAccount" />
        <activity android:name=".SettingsMenuDish" />
        <activity android:name=".SettingsMenuAdd" />

    </application>

</manifest>